name: Fortify-Scan

on: push

jobs:
  fortify-SAST:
    runs-on: 
      - self-hosted
    steps:
      - name: Check out source code
        uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
      #  with:
      #    python-version: '3.11.2'
      - name: Probar scripts
        shell: cmd
        run: |
          hostname
          python --version
        #isSSCOnline = $(py C:/pipeline/online.py)
        #echo $isSSCOnline
        #scancentral -sscurl $SSC_URL -ssctoken $SSC_TOKEN start -bt none -upload -application $APPLICATION -version $VERSION -uptoken $SSC_TOKEN
      #- name: Run ScanCentral SAST Scan
       # uses: fortify/github-action@v1
       # with:
       #   sast-scan: true
        #env:
         # SSC_URL: ${{secrets.SSC_URL}}
          #SSC_TOKEN: ${{secrets.SSC_TOKEN}}
          #SC_SAST_CLIENT_AUTH_TOKEN: ${{secrets.CLIENT_AUTH_TOKEN}}
          #SSC_APPVERSION: test:qa
          #EXTRA_SC_SAST_SCAN_OPTS: -k
          
